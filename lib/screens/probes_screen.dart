import 'package:flutter/material.dart';

class ProbesScreen extends StatelessWidget {
  const ProbesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
